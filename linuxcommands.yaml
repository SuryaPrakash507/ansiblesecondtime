---
 - hosts: testnodes
   become: yes
   gather_facts: yes
   tasks:
   - name: getting host details of node 2 and node 3
     cmd: sudo cat /etc/os-release
     register: hostdetails


   - name: printing the variable data of node 2 and node 3
     debug:
      msg: "{{hostdetails}}"